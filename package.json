{
  "name": "snip-bundle",
  "version": "1.0.0",
  "description": "Snip – self-contained bundle (backend + frontend)",
  "scripts": {
    "start": "bun server.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
