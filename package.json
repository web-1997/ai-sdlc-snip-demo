{
  "name": "snip-backend",
  "version": "1.0.0",
  "description": "Snip – tiny URL shortener backend (Bun, zero npm dependencies)",
  "type": "module",
  "scripts": {
    "start": "bun run server.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
